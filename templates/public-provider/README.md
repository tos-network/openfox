# Public Provider and Gateway

Use this template when you want to run:

- one public gateway
- one provider behind NAT or on a private LAN
- paid provider surfaces exposed through the gateway

Files:

- `provider.openfox.json`
- `gateway.openfox.json`

Replace placeholder addresses, URLs, and bootnodes before use.
